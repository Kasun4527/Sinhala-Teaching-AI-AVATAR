# Copy this file to ".env" in this same directory on the server and fill in real values.
# ".env" is gitignored — never commit it.

# Origin allowed to POST frames to this service (CORS), e.g. http://<frontend-ip>:3000
FRONTEND_URL=
