__pycache__/
*.pyc
.env
.venv/
venv/
test/
test_retriever.py
.git
.gitignore
*.md

# Served via the Azure Files mount at runtime (see azure-setup.sh /
# containerapp volumeMounts) — not needed inside the image itself, and
# excluding them keeps the ACR build-context upload small.
documents_unicode/
images/
avtr_cache/
